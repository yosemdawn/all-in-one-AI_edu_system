三、完整手工测试顺序
按这个顺序测，最不容易乱。

1. 管理员登录与系统状态
操作：

用 admin / 123456 登录
打开管理员首页/仪表盘
查看用户总数、班级总数、作业总数、AI 模型状态
进入 AI 模型管理页，找到 doubao
点“测试连接”或类似按钮
进入日志页面，确认能看到接口日志
预期：

能登录成功
仪表盘有数据
AI 模型测试返回成功或连接正常
日志页能看到记录
2. 教师登录与班级功能
操作：

退出管理员，改用 teacher1 / 123456 登录
打开班级管理
查看系统自带的演示班级
新建一个班级，比如 测试班级A
查看班级详情
点击“重置邀请码/重新生成班级码”之类功能
预期：

能看到班级列表
新建成功
班级详情可打开
能拿到一个班级邀请码
3. 学生加入班级
操作：

退出教师，改用 student1 / 123456 登录
打开“加入班级”
输入刚才教师生成的邀请码
提交加入
返回“我的班级”查看
预期：

加入成功
学生班级列表里能看到该班级
4. 教师发布作业
操作：

切回教师账号
打开作业管理
新建一个作业
绑定到刚才的班级
填标题、描述、开始时间、截止时间
发布作业
预期：

作业创建成功
状态从草稿变成已发布
作业列表里能看到它
5. 学生查看作业
操作：

切回学生账号
打开“我的作业”
找到刚刚发布的作业
打开作业详情
预期：

能看到新作业
能看到作业标题、描述、截止时间
页面显示可提交
6. 学生先存草稿，再正式提交
操作：

在作业提交页先输入一段内容
先点“保存草稿”
再进入该作业确认草稿存在
然后再点“正式提交”
预期：

草稿保存成功
再次打开时能看到草稿内容
正式提交成功
提交记录里状态更新
7. AI 批改专项测试
这是最关键的一步。

操作：

学生正式提交后，等待 5-30 秒
学生刷新“我的提交”或作业详情
教师打开“提交列表”查看这份提交
教师查看是否出现 AI 分数、AI 评语、AI 状态
预期：

不应该再看到 queue_disabled
提交应进入队列，然后出现 AI 批改结果
学生侧能看到 AI 评语或 AI 分数
教师侧能看到该提交已被 AI 处理过
如果这一步失败，先看日志：

docker compose -f compose.prod.yml logs -f backend
重点看是否有：

DOUBAO_API_KEY is not configured
Redis 连接失败
Doubao 请求失败
8. 教师人工批改
操作：

教师打开某个学生提交
填教师评语
填教师分数
提交批改
预期：

批改成功
学生再次打开自己的提交时，能看到教师评语和教师分数
状态变成教师已批改
9. AI 规则功能
操作：

教师进入 AI 规则管理
新建一条 AI 规则
查看详情
修改名称或状态
复制一份规则
删除复制出来的规则
预期：

增删改查都正常
可用规则列表里能看到你创建的规则
10. 用户管理与权限功能
操作：

切回管理员
新建一个教师或学生账号
修改该账号状态为启用/禁用
重置该用户密码
进入角色管理
新建一个角色
进入菜单管理
新建一个菜单
把角色分配给用户
预期：

用户可以创建、修改、删除
密码重置后新密码可登录
角色和菜单都能操作
分配后资源权限能变化
11. 找回密码功能
操作：

在登录页点击“忘记密码”
输入一个存在的邮箱
如果前端有显示 reset token 的开发模式结果，就拿它去重置密码
再用新密码登录
预期：

忘记密码接口成功
重置密码成功
新密码可登录
12. 登出与重新登录
操作：

任意账号登录后点退出
再访问个人资料页
再重新登录
预期：

退出后旧登录态失效
重新登录正常
13. 管理员查看日志
操作：

切回管理员
打开日志页
搜索 /api/v1/auth/login
搜索重置密码接口
搜索 AI 模型更新接口
预期：

能查到日志
敏感字段应该被打码，比如：
password
token
refreshToken
resetToken
apiKey
四、如果你只想先快速判断“核心功能是否正常”
你最少测这 6 步就够了：

管理员登录成功
教师创建班级成功
学生用邀请码加入班级成功
教师发布作业成功
学生提交作业成功
AI 批改和教师批改都能看到结果
这 6 步都通了，说明主业务闭环基本没问题。

五、如果出问题，你优先看这几个地方
先执行：

docker compose -f compose.prod.yml logs -f backend
docker compose -f compose.prod.yml logs -f mongo
docker compose -f compose.prod.yml logs -f redis
然后看报错属于哪类：

登录失败：多半是数据库/seed/密码问题
看不到演示账号：多半是没清空卷或没开 ENABLE_DEMO_SEED=true
AI 没结果：多半是 DOUBAO_API_KEY、Redis、队列、外网请求问题
前端请求失败：多半是 CORS_ORIGINS 或前端 API 地址不对
六、最建议你的实际做法
照这个顺序来：

先确认 healthz 正常
先登录 admin
再登录 teacher1
再登录 student1
按“建班 -> 加班 -> 发作业 -> 提交 -> AI 批改 -> 教师批改”走一遍