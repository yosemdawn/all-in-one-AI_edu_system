# HTTP 301 Redirect to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name yosem.vip www.yosem.vip;

    # Certbot root for validation
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://yosem.vip$request_uri;
    }
}

# HTTPS Server
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name yosem.vip;

    # SSL Certificates (Placeholder for Certbot paths)
    # These will be generated by running: certbot --nginx -d yosem.vip -d www.yosem.vip
    ssl_certificate /etc/letsencrypt/live/yosem.vip/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yosem.vip/privkey.pem;

    # Optimized SSL settings
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # HSTS
    add_header Strict-Transport-Security "max-age=63072000" always;

    # Proxy to Frontend Docker Container
    # Note: Ensure docker-compose.yml maps frontend to port 8080 on localhost
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Max upload size
        client_max_body_size 50M;
    }

    # Optional: Log settings
    access_log /var/log/nginx/yosem.vip.access.log;
    error_log /var/log/nginx/yosem.vip.error.log;
}

# Redirect www to non-www (HTTPS)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.yosem.vip;

    ssl_certificate /etc/letsencrypt/live/yosem.vip/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yosem.vip/privkey.pem;

    return 301 https://yosem.vip$request_uri;
}
